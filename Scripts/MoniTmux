#!/bin/bash

sessionName=monitor

tmux new-session -s "$sessionName" -n "$sessionName"  -x - -y - -d 'docker stats'

tmux splitw -v -p 90 -t "$sessionName" 'htop'
tmux splitw -v -p 90 -t "$sessionName" 'bmon'
tmux send keys "id"

tmux -2 attach-session -t "$sessionName"
